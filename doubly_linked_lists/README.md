# DOUBLY LINKED LISTS


## 📑 DESCRIPTION

En informatique, une double linked list (liste doublement chaînée en français) est une structure de données linéaire qui permet le stockage et la manipulation de données de manière efficace.

Contrairement à une singly linked list, une double linked list contient des nœuds qui possèdent des liens vers les nœuds précédents et suivants. Chaque nœud contient des données et deux pointeurs : un pointeur vers le nœud précédent et un pointeur vers le nœud suivant. Le premier nœud de la liste, appelé "tête", a un pointeur vers le nœud suivant mais pas vers le nœud précédent. Le dernier nœud de la liste, appelé "queue", a un pointeur vers le nœud précédent mais pas vers le nœud suivant.

Les double linked lists offrent plusieurs avantages par rapport aux singly linked lists. En particulier, les opérations d'insertion et de suppression sont plus rapides dans une double linked list car elle permet d'accéder facilement aux nœuds précédents et suivants. En revanche, la double linked list utilise plus de mémoire car chaque nœud stocke des pointeurs supplémentaires.

----------------------
## 🔧 STACKS

    LANGAGE C

----------------------
## 💻 SUIVANT

  - 🗒 [Hash tables](https://github.com/RedginaldGodeau/holbertonschool-low_level_programming/tree/main/)
----------------------
## 👦 Authors

- [@Redginald Godeau](https://github.com/RedginaldGodeau)
