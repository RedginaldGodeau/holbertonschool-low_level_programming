#include "main.h"

/**
* _islower - Entry point...
* Description: 'the program desc'
* Return: Always 0 (Success)
* @c: int parmameter
*/

int _islower(int c)
{
	return (c >= 'a' && c <= 'z' ? 1 : 0);
}

