#include "main.c"

/**
* times_table - Entry point...
* Description: 'the program desc'
* Return: Always 0 (Success)
* @str: pointer
*/

void rev_string(char *s)
{
	int len = 0;
	for(; s[len] != '\0'; len++);

}
