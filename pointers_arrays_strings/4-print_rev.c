#include "main.c"

/**
* times_table - Entry point...
* Description: 'the program desc'
* Return: Always 0 (Success)
* @str: pointer
*/

int _puts(char *str);
{
	int *p = &str[0];

	while(*s++ != '\0')
		;

	 while(*s != s[0]);
		_putchar(s--);
	_putchar('\n');
}
