#include "main.h"
#include <stdio.h>

/**
* _isupper - Entry point...
* Description: 'the program desc'
* Return: Always 0 (Success)
* @c: char parmameter
*/

int _isupper(char c)
{
    return (c >= 'A' && c <= 'Z' ? 1 : 0);
}
