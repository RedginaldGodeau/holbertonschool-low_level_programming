#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
	char list = 'a';
	for(int i = 0; list[i] != z; i++ )
		putchar (list);
	putchat (list);

	return (0);
}
